# PostgreSQL deployment for DineOps
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: dineops
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          env:
            - name: POSTGRES_DB
              value: dineops
            - name: POSTGRES_USER
              value: dineops
            - name: POSTGRES_PASSWORD
              value: dineops123
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: postgres-storage
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: postgres-storage
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: dineops
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
